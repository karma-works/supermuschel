<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow JIT compilation (required for V8/Node.js inside Electron) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required for node-pty / spawn-helper) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Disable library validation so native addons (better-sqlite3, node-pty) can load -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Network client access -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- File access — needed for project directory reads/writes -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Allow dyld env vars (needed for some native module loading) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
  </dict>
</plist>
