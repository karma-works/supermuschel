// Claude Code specific helpers
export const CLAUDE_INSTALL_INSTRUCTIONS = `
Claude Code is not installed or not in PATH.

Install it with:
  npm install -g @anthropic-ai/claude-code

Or via your preferred package manager. Make sure 'claude' is available in your PATH.
`.trim();
