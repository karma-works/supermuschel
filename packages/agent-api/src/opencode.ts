// OpenCode specific helpers
export const OPENCODE_INSTALL_INSTRUCTIONS = `
OpenCode is not installed or not in PATH.

Install it with:
  curl -fsSL https://opencode.ai/install | sh

Or via Homebrew:
  brew install sst/tap/opencode

Make sure 'opencode' is available in your PATH.
`.trim();
